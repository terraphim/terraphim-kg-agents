# terraphim-kg-agents

Knowledge-graph agent crates extracted from terraphim-ai (#1910):

- `terraphim_kg_agents` -- KG-driven agent implementations
- `terraphim_kg_linter` -- KG linting over codebases
- `terraphim_codebase_eval` -- codebase evaluation primitives

Consumes upstream crates from the `terraphim` Gitea cargo registry.
Licensed Apache-2.0.
